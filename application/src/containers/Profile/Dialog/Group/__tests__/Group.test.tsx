/*
 * @Author: Devin Lin (devin.lin@ringcentral.com)
 * @Date: 2018-11-27 09:50:18
 * Copyright © RingCentral. All rights reserved.
 */

// import React from 'react';
// import { shallow } from 'enzyme';
// import { ProfileDialogGroup } from '../Group';
// import {
//   JuiDialogTitleWithAction,
//   JuiDialogContentWithFill,
// } from 'jui/components/Dialog';
// import { ProfileDialogGroupTitle } from '../Title';
// import { ProfileDialogGroupContent } from '../Content';

describe('ProfileDialogPerson', () => {
  describe('render()', () => {
    it('should be render title and content component when instance component', () => {
      // const Wrapper = shallow(<ProfileDialogGroup id={1} dismiss={() => {}} />);
      // expect(Wrapper.find(JuiDialogTitleWithAction)).toHaveLength(1);
      // expect(Wrapper.find(JuiDialogContentWithFill)).toHaveLength(1);
      // expect(Wrapper.find(ProfileDialogGroupTitle)).toHaveLength(1);
      // expect(Wrapper.find(ProfileDialogGroupContent)).toHaveLength(1);
    });
  });
});
